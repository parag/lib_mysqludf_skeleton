UDF Repository for MySQL
lib_mysqludf_skeleton

-----------------------

-----------------------
To configure: 
 autoreconf
 make sure that you have required libraries installed
 you may require to install libtool, libmysqlclient16-dev
To build:
 ./configure
 make && make install

To then load the functions:
 mysql -u root -p < installdb.sql

To remove the library from your system:
 mysql -u root -p < uninstalldb.sql
 make uninstall
